version: "3.8"
services:
  api-instagram:
    build: .
    image: mateusgomes1/pipegram:latest
    ports:
      - "3000:3000"
    volumes:
      - ./sessions:/app/sessions
    env_file:
      - .env
    restart: unless-stopped
